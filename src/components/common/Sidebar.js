import React, { useState, useEffect, useContext } from 'react';

const Sidebar = (props) => {

    return (
        <React.Fragment>
            <div>
              <h1>SIDEBAR</h1>
            </div>
        </React.Fragment>
    );
}

export default Sidebar;